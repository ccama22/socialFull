import { Navigate, Outlet } from "react-router-dom";
import React from "react";

const PublicRoutesRegister = () => {
  return (
    <>
      <Outlet />
    </>
  );
};

export default PublicRoutesRegister;
